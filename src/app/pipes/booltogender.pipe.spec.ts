import { BooltogenderPipe } from './booltogender.pipe';

describe('BooltogenderPipe', () => {
  it('create an instance', () => {
    const pipe = new BooltogenderPipe();
    expect(pipe).toBeTruthy();
  });
});
